# Knowledge Log

## 2026-07-24
* **Harvest**: merged pending note `desktop-succession-boundaries.md` into the existing [desktop succession decision](/decisions/desktop-panel-succession.md); no concept change was needed because the decision already carried the durable boundaries.
* **Creation**: [Desktop succeeds the web and terminal panels as a standalone product](/decisions/desktop-panel-succession.md) — the Electron app becomes the sole panel and GitHub-Release product; its package owns the backend, installed `oas ... --json` gates administration, no-OAS mode observes existing deployments, and a stability-gated N/N+1 release sequence retires oas.web and oas pane after soul knowledge succession.
* **Deprecation**: the [web pane](/decisions/web-pane.md), [standalone TUI](/decisions/control-pane-live-standalone-tui.md), [card architecture](/decisions/control-pane-v3-card-architecture.md), and [visual language](/decisions/control-pane-visual-language.md) decisions are retained as historical migration sources but superseded for continuing product direction by the desktop succession decision.
* **Update**: the [roadmap](/roadmap/next-steps.md) now leads with desktop succession and renames the telemetry thread for the successor; [repo state](/stewardship/repo-state.md) records the feature integration and three follow-up briefs in flight.

## 2026-07-23
* **Update**: skills/pr-review — added window-gone/no-verdict triage: check all-mail history and session logs before diagnosing a vanished reviewer as killed.
* **Creation**: [Vanished agent window + no verdict — completed-but-event-dropped vs killed](/lessons/window-gone-completed-vs-killed-triage.md) — a vanished tmux window with no verdict awakening can mean clean self-retirement plus a channel delivery fault; distinguish it from an external kill via `aw mail inbox --show-all` and the session log tail.
* **Update**: skills/pr-review — added the bare `node --test` discovery-scope gotcha for stale sibling worktrees and suspiciously high test counts.
* **Creation**: [Bare `node --test` recurses into agent worktrees — runs stale sibling test suites](/lessons/bare-node-test-recurses-into-agent-worktrees.md) — bare `node --test` walks nested agent work checkouts; root test scripts must pin explicit globs to avoid running stale sibling suites.
* **Update**: skills/pr-review — added the tmux target prefix-matching review gotcha for spawn/retire tests and exact `=<session>:=<window>` targeting.
* **Creation**: [tmux target prefix matching can kill foreign windows](/lessons/tmux-target-exact-matching.md) — tmux `-t session:window` can prefix-match sibling windows; retire paths must use exact targets and tests must avoid real sessions.

## 2026-07-22
* **Update**: skills/pr-review — added same-capability conflict-return staleness guidance and clarified that same-account GitHub auth also blocks request-changes reviews.
* **Update**: [PR review friction — same-account review states and worktree-held branch deletion](/lessons/pr-review-same-account-and-worktree-branch-delete.md) — clarified that the same-account review block applies to `gh pr review --request-changes` as well as approvals.
* **Creation**: [Returned PRs can go stale under fast same-capability cadence](/lessons/pr-return-staleness-fast-capability-cadence.md) — conflict-only returns can re-conflict before re-review when another same-capability PR lands; handback should re-merge main and re-check mergeability.
* **Update**: skills/pr-review — added scratch-worktree gate environment setup before judging PR failures.
* **Creation**: [Scratch-worktree PR gates need dependencies and installed capabilities](/lessons/scratch-worktree-pr-gate-environment.md) — bare scratch worktrees can fail `npm run validate` without devDependencies and oas-web `/api/agents` without deployment installed capabilities, even when the PR is correct.
* **Update**: skills/pr-review — added same-account approval fallback and worktree-held branch deletion recovery gotchas.
* **Creation**: [PR merge friction — same-account approval and worktree-held branch deletion](/lessons/pr-review-same-account-and-worktree-branch-delete.md) — single-account GitHub auth blocks `gh pr review --approve`; `gh pr merge --delete-branch` can leave local branch deletion to the owning worktree.
* **Creation**: [stewardship/](/stewardship/) section — repo-state.md (living on-main/in-flight/open-threads picture) and delivery-log.md (append-only per-PR verdicts); maintenance contract: every instance feeds it, per-PR maintainers append before retiring.
* **Creation**: [Release bump PR failure is an org-level Actions restriction](/lessons/release-bump-pr-org-restriction.md) — `createPullRequest` failures after publish are an OAS-Framework organization Actions restriction; check npm publish state and manually create/merge the release-bump PR until an org admin changes the policy.
* **Update**: skills/git-tag-release — added release-bump PR failure recovery and the Pi package-name removal gotcha.

## 2026-07-21
* **Creation**: skills/retrofit-aweb-identity — added the live-instance aweb identity retrofit procedure, including replaying the oas-aweb spawn hook, cleaning `undefined`, and persisting retire metadata.
* **Creation**: skills/capability-source-reinstall — added the local capability source reinstall procedure and gotchas about path installs, trust, hoisted manifest paths, and version bumps.
* **Update**: [OAS development team — PR-only flow, review capability, capability-defined agents, model preference lists](/decisions/dev-team-and-review-flow.md) now links reviewer protocol to the multi-agent failure-modes lesson.
* **Creation**: [Multi-agent runs need exact messaging and reviewer protocols](/lessons/multi-dev-run-failure-modes.md) — first coordinator-plus-developers run failures around aweb polling, CLI flags, inbox semantics, reviewer ephemerality, and exact command injections.
* **Update**: [OAS development team — PR-only flow, review capability, capability-defined agents, model preference lists](/decisions/dev-team-and-review-flow.md) now links model preference lists to the Copilot auth fragility lesson.
* **Creation**: [Copilot-proxied models fail mid-session — plan for auth death, not just spawn-time fallback](/lessons/copilot-auth-fragility.md) — spawn-time model fallbacks do not protect long-running sessions from Copilot OAuth/proxy/provider errors, and panel sends need post-send error surfacing.
* **Update**: [dev team decision](/decisions/dev-team-and-review-flow.md) amended — the maintainer commits directly to main; PR-only applies to the dev team.
* **Creation**: [OAS development team — PR-only flow, review capability, capability-defined agents, model preference lists](/decisions/dev-team-and-review-flow.md) — v0.16.0 contracts + the team + oas.review + maintainer pr-review skill; developer knowledge seeded from pi sessions by fan-out workflow.

## 2026-07-20
* **Creation**: [Terminal theme inference for TUIs](/lessons/tui-theme-inference.md) — promoted the Control Pane TUI lesson that OSC 11 should drive light/dark detection, COLORFGBG is the fallback, and semantic palettes must swap as a whole.
* **Update**: [Control Pane visual language](/decisions/control-pane-visual-language.md) now links to the terminal theme inference lesson for palette and terminal-background handling.

## 2026-07-17
* **Creation**: [Web pane — browser control panel as the oas.web marketplace capability](/decisions/web-pane.md) — the agents' Slack: roster + live sessions + type-into-terminal + Jira drawer, shipped as oas.web v0.1.0.
* **Creation**: [Workspace work mode for cross-repo coordinators](/decisions/workspace-work-mode.md) — fourth work mode: ./work = team scope, read-all edit-none discipline, soul knowledge lands as PRs to the soul's home repo (oas.okf v1.1.0 harvest branch+PR flow).
* **Creation**: [Init-time acquisition cannot rely on config-chain discovery](/lessons/init-acquisition-discovery-gotcha.md) — promoted the init-time gotcha that commands acquiring capabilities before writing config must use the acquisition result directly instead of rediscovering through config-chain manifest lookup.
* **Creation**: [Marketplace over bundled, work modes simplified, runtime integration at spawn](/decisions/marketplace-workmodes-runtime.md) — v0.13.0 breaking set: marketplace acquisition replaces bundled origin, work-mode overrides removed (setup: stays), init prompts per layer in a TTY, claude runtime wired end-to-end (oas-claude-config + aweb channel plugin via hook launch args).

## 2026-07-15
* **Creation**: [Cross-repo spawn belongs in CLI root resolution, not the kernel](/lessons/cross-repo-spawn-cli-root-resolution.md) — promoted the implementation lesson that team-scope cross-repo spawn/retire belongs in CLI root selection, leaving `spawnInstance`/`retireInstance` untouched and preserving soul-repo homing.
* **Update**: [Team as a first-class config entity](/decisions/team-as-config-entity.md) — cross-repo spawn/retire implemented (team-scope soul/instance lookup, unique match wins, homing stays with the soul's repo); `team.repos:` closed as not needed (the scope's tree is the member list).

## 2026-07-14
* **Update**: [Team as a first-class config entity](/decisions/team-as-config-entity.md) — added onboarding/degradation: `oas aweb setup` guided flow, graceful no-aw spawn, `oas update` + doctor-as-code for framework updates.
* **Creation**: [Team as a first-class config entity](/decisions/team-as-config-entity.md) — `team:` (name + optional id) declares the deployment boundary; drives identity, `oas status --team` cross-repo discovery, and the aweb join preference chain (config id > name > settings pin > active team).
* **Creation**: [Config authorship completeness and ambient skill coexistence](/decisions/config-authorship-and-ambient-skills.md) — injection→injection-override (forbidden on owned/path), oas type / inject eject / use --settings, ambient skills coexist (restriction flags dropped), oas skill split into oas + oas-config.
* **Update**: [implementation](/architecture/implementation.md), [skill layering](/architecture/skill-layering.md), [standalone CLI](/decisions/standalone-cli.md), and [roadmap](/roadmap/next-steps.md) now clarify that `@oas-framework/pi` is a runtime bridge for memory session events/resources/bootstrap, while skill resolution belongs to spawn-time materialization plus harness exclusion flags.

## 2026-07-13
* **Creation**: [parseYamlNested treated comment-only values as scalars](/lessons/yaml-parser-comment-only-values.md) — promotes the parser gotcha that comment-only values such as `layers:  # comment` must open nested maps, and that doc-example schema failures can reveal parser bugs.
* **Creation**: [Config shape v2 — agent types in souls, layered capabilities, conventional injection overrides](/decisions/config-shape-agent-types-and-injections.md) — groups→agent-types with soul-declared membership, capabilities split into layers/additive, `from` provenance, commented `injection:` overrides, CLI as config author.
* **Update**: [oas-config](/architecture/oas-config.md) rewritten for the v2 schema (agent-types, layer slots, from, injection conventions, removed spellings).
* **Creation**: [npm EOTP failure in tag-driven release CI](/lessons/npm-eotp-in-tag-release.md) — EOTP at npm publish means the CI token is 2FA-constrained; fix with a granular package read/write token and rerun failed jobs without retagging.
* **Update**: skills/git-tag-release — added EOTP/NPM_TOKEN failed-run recovery and recorded that repo renames do not affect npm publish authority.

## 2026-07-12
* **Creation**: [Scoped capability store, restorable installs, and config templates](/decisions/scoped-capability-store-and-templates.md) — one config-scoped store (`installed/` vs `owned/`), bare `oas install` restore, snapshot templates, per-capability injection overrides; resolves the lock-cache-sharing watch item.
* **Update**: [implementation](/architecture/implementation.md) and [roadmap](/roadmap/next-steps.md) reflect the scoped store, restore, templates, and injection overrides; `~/.oas/capabilities/` removed from the canonical account.

## 2026-07-11
* **Creation**: [Control Pane v3 card architecture](/decisions/control-pane-v3-card-architecture.md) — promotes the live redesign decision to a single identity-rail card stack with in-place expansion, variable-height scrolling, and full-screen zoom.
* **Update**: [Control Pane visual language](/decisions/control-pane-visual-language.md) now points to the v3 card architecture as the successor to the list badge and two-panel implications.
* **Creation**: [Control Pane visual language](/decisions/control-pane-visual-language.md) — soul badges, branch chips, tree glyphs, three-line rows, selection, and case discipline from the July 2026 redesign.
* **Creation**: [Iterate on Control Pane visuals with a mock render script](/playbooks/control-pane-mock-render-iteration.md) — fabricated snapshots let future Control Pane UI work exercise `renderFrame` without spawning live agents.
* **Update**: [Control Pane decision](/decisions/control-pane-live-standalone-tui.md) — spawn origin distinguishes intentional operator roots from missing legacy lineage; the Agent Map labels legacy instances unlinked instead of presenting unknown provenance as hierarchy.
* **Creation**: [Control Pane is a live standalone TUI](/decisions/control-pane-live-standalone-tui.md) — `oas pane` uses a runtime-neutral current-state model and isolated ANSI frontend; forward-only lineage shapes the constellation and telemetry waits for an adapter-neutral contract.
* **Update**: [roadmap](/roadmap/next-steps.md) marks the live Control Pane MVP done and narrows the follow-up to runtime-neutral telemetry.
* **Update**: [Package smoke tests must cross the checkout boundary](/lessons/package-smoke-tests-cross-checkout-boundary.md) now records the Pi directory-symlink skill discovery gotcha and the required real-harness resource report shape.
* **Update**: skills/git-tag-release now requires Pi adapter/resource release probes to verify selected instruction and skill visibility, unrelated ambient skill absence, and real materialized skill directories.
* **Update**: capability Decision and architecture records now define the first clean public contract, supersede pre-release workspace/config migration shapes, retain only `layers.<layer>: none`, and require real packed-harness verification of adapter resource visibility.
* **Update**: [Package smoke tests must cross the checkout boundary](/lessons/package-smoke-tests-cross-checkout-boundary.md) now distinguishes deterministic packed smoke from a disposable real harness session that proves selected resources are actually visible.
* **Update**: skills/git-tag-release now uses canonical capability IDs and requires generated-instance/exact-skill checks plus a real pi probe for adapter changes.
* **Update**: skills/git-tag-release — added the package-smoke checkout-boundary preflight for package contents, bin entry points, adapter/kernel resolution, and release smoke changes.
* **Creation**: [Package smoke tests must cross the checkout boundary](/lessons/package-smoke-tests-cross-checkout-boundary.md) — source-tree probes do not prove npm file lists, bin links, or thin-adapter kernel resolution; tarball smoke tests must install both artifacts outside the checkout.
* **Update**: [Ambient harness skill discovery cannot provide exact agent isolation](/lessons/skill-discovery-repo-boundary.md) — merged Pi-specific gotcha: `resources_discover` is additive, so exact isolation requires `--no-skills --skill <instance>/.agents/skills` and launch-command tests.
* **Creation**: [Acquisition provenance must outrank installation location for trust](/lessons/acquisition-provenance-trust.md) — local capability dirs do not make acquired packages config-owned; lock provenance drives integrity and executable trust.
* **Update**: capability-package implementation landed in architecture records — [implementation](/architecture/implementation.md), [oas-config](/architecture/oas-config.md), [skill layering](/architecture/skill-layering.md), [kernel/providers](/decisions/kernel-and-providers.md), and [standalone CLI](/decisions/standalone-cli.md) now describe exact instance-local composition, config-owned soul targets, and lock/trust; the old repo-boundary bridge lesson is superseded by [ambient discovery cannot isolate agents](/lessons/skill-discovery-repo-boundary.md).
* **Creation**: [Capability packages and instance-local composition](/decisions/capability-packages.md) — generalize integrations into targetable, locked capability packages with exact instance-local skill/instruction composition while preserving formally defined exclusive knowledge, messaging, and tasks layers.

## 2026-07-10
* **Creation**: [Instances symlink the soul rather than copy it](/decisions/soul-knowledge-symlink-rationale.md) — promoted from `oas-expert-first-boot` notes; records why instance homes link `./soul` to the shared soul instead of copying knowledge.
* **Creation**: [OKF injection was write-biased — read-side consultation must be explicit](/lessons/okf-injection-read-side-gap.md) — promoted from `oas-expert-first-boot` notes; records the v0.6.2 read-side memory contract fix.
* **Update**: [Memory design](/architecture/memory-design.md) now links the explicit consult-index-first lesson and the soul-symlink decision.
* **Harvest**: `release-no-local-version-bump.md` merged into existing `skills/git-tag-release` procedure; the no-local-pre-bump gotcha and failed-run recovery were already captured there.
* **Creation**: [npm package export maps must expose package.json](/lessons/npm-package-exports-and-files.md) — promote packaging gotcha from `oas-expert-first-boot` notes: export `./package.json` when adding an npm exports map, and add a `files` whitelist before publishing.
* **Update**: the CLI became the universal command surface (v0.3.0→v0.6.1, all CI-published): roster commands + --json everywhere; pi adapter stripped to glue (no tools); skills/injections/docs teach CLI only; [implementation](/architecture/implementation.md) rewritten, [standalone-cli](/decisions/standalone-cli.md) status updated, roadmap refreshed; new lesson [release-verification](/lessons/release-verification.md) (v0.6.1 backtick SyntaxError; CI now node --checks every shipped .mjs).
* **Curation**: soul globalized for worldwide use — deployments/ section retired (deployment-specific state does not travel with the framework repo); all concepts generalized (examples use placeholder names); jira-over-aweb decision generalized to "the tasks layer owns tasks"; roadmap rewritten framework-only; AGENTS.md gains the globalization rule.

## 2026-07-09
* **Update**: agent-memory skill retired — instances capture (okf injection: STATE/log/notes + commit), harvesters judge (memory-harvest skill: bar, types, routing); [memory-design](/architecture/memory-design.md) updated. All framework references swept clean.
* **Update**: harvest redesigned — continuous post-commit (memory-harvest agent, attached work mode, knowledge-vs-skill routing, loop guard/debounce) replaces retirement harvest + inbox; retirement is a knowledge no-op; [memory-design](/architecture/memory-design.md) rewritten; E2E 8-point matrix green.
* **Update**: stage (d) landed — the `oas` CLI (doctor/use/init) in bin/oas.mjs, reusing kernel resolution exports; setup is now one command instead of hand-edited YAML; [roadmap](/roadmap/next-steps.md) updated. Docs deferred to the running docs-expert audit (shared checkout, no edit collision).
* **Fix**: [architecture/implementation.md](/architecture/implementation.md) rewritten — was still describing the pre-migration global extension; now covers package-in-repo, memory-agnostic kernel, integrations, scoping. [knowledge-typology](/architecture/knowledge-typology.md) encoding section + [sections decision](/decisions/workspace-seeded-knowledge-sections.md) updated for okf-owned mechanisms.
* **Update**: kernel made fully memory-agnostic — all memory conventions (soul knowledge scaffold, STATE.md/log.md/notes/, harvest, TASK.md memory brief, agent-memory skill) moved into integrations/oas-okf (soul-scaffold/spawn/retire hooks); `knowledge: none` ⇒ no memory machinery; session automation self-gates on STATE.md existing; [memory-design](/architecture/memory-design.md) + [kernel-and-providers](/decisions/kernel-and-providers.md) + docs updated; E2E matrix green (okf on: scaffold+brief+harvest; none: nothing, task intact; skills scoped).
* **Update**: custom integrations landed — three distribution paths (bundled / `<level>/.agents/integrations/` auto-discovered / own-repo via `path:`), origin-tracked manifests, closer-level precedence; [oas-config](/architecture/oas-config.md) + docs/integrations.md updated; E2E-tested (local tasks integration, path-referenced messaging with unmet requires).
* **Creation**: integrations-expert agent — second soul in this repo (checkout mode): builds custom integrations with users; four skills (integration-craft, tasks/messaging/knowledge-integration) referenced from its AGENTS.md; scaffold probe green (4 injection blocks, skills linked).
* **Update**: packages/ renamed integrations/ — manifests now declare layer + description + `requires` (external deps checked by kernel, unmet → spawn warning with install pointer); config key `integrations:` (providers: legacy alias); [decision](/decisions/kernel-and-providers.md) + [oas-config](/architecture/oas-config.md) updated.
* **Update**: stage (c) landed — provider packages (packages/oas-okf, packages/oas-aweb with oas.json manifests), generic lifecycle hooks (env+JSON contract, meta round-trip), workspace-bounded aweb team resolution recorded in [lesson](/lessons/aweb-workspace-lifecycle.md); provider skills now workspace-scoped by cascade; [roadmap](/roadmap/next-steps.md) updated.
* **Update**: [oas-config](/architecture/oas-config.md) — injection key renamed `agents-md-injection`, markers record src= source files, work-mode briefings are SOUL AGENTS.md injections (work mode is soul identity, fixed at creation) not TASK.md appends; implemented, tested (wt/co souls get exactly their mode's block, TASK.md clean), live soul refreshed, pushed.
* **Update**: package-in-repo + config cascade landed — implementation moved into the framework repo (extension/, skills/, injects/, package.json), installed via `pi install`; oas-config.yaml cascade implemented (per-key, shim, reconciled oas:* injections, work-mode briefings, worktree setup:); laptop (messaging: aweb) + repo (knowledge: okf) configs live; [roadmap](/roadmap/next-steps.md) stages a–b closed; [implementation](/architecture/implementation.md) + repo docs updated. Soul AGENTS.md migrated to oas:* markers.
* **Harvest**: [lessons/aweb-workspace-lifecycle.md](/lessons/aweb-workspace-lifecycle.md) promoted directly from instance notes (Finding → Lesson; verified E2E, encoded in retire) ahead of instances/ being gitignored; deployment janitorial TODOs moved to the deployment record (since retired from this bundle).
* **Update**: [kernel-and-providers](/decisions/kernel-and-providers.md) + [oas-config](/architecture/oas-config.md) refined per human: config at level ROOT (`<level>/oas-config.yaml`, not .agents/); providers stated plainly as skills+instructions; work-modes config block (per-mode default injections distilled from a2am developer/maintainer souls + worktree `setup:` command).
* **Creation**: public-facing docs written in the framework repo — README.md + docs/{layers,souls-and-instances,knowledge,packages,configuration,conventions}.md — describing the five layers, kernel/provider split, oas-config resolution, canonical-file conventions; marked as target architecture with status note.
* **Creation**: [decisions/kernel-and-providers.md](/decisions/kernel-and-providers.md) + [architecture/oas-config.md](/architecture/oas-config.md) — OAS becomes kernel + provider packages (oas-okf, oas-aweb); three-level oas-config.yaml cascade designed; [workspace-config.md](/architecture/workspace-config.md) superseded; [what-oas-is.md](/architecture/what-oas-is.md) now names the five layers; staged migration on [roadmap](/roadmap/next-steps.md).

## 2026-07-08
* **Update**: aweb integration landed in the package — awebConfig/mintAwebIdentity in core.mjs, spawn mints + TASK.md comms line + instance.json metadata, retire self-deletes; default-on opt-out via [workspace config](/architecture/workspace-config.md); roadmap thread 1 closed. E2E probe on a live hosted team: mint→roster→retire→roster-clean, awebRetired:true.
* **Update**: first-deployment record + roadmap — aweb human-account email claim resolved for the deployment's hosted team (`aw claim-human` from the workspace root).
* **Creation**: [decisions/workspace-seeded-knowledge-sections.md](/decisions/workspace-seeded-knowledge-sections.md) — workspace.yaml `knowledge-sections` key (additive-only, creation-only) implemented in core.mjs; [workspace-config.md](/architecture/workspace-config.md) and package README/agent-memory updated.
* **Fix**: [knowledge-typology.md](/architecture/knowledge-typology.md) provenance tier 1 corrected — `Playbook`/`Reference` are spec-exampled (non-normative, in SPEC.md), not community-inherited; only `Lesson`/`Decision` are. [okf-spec.md](/references/okf-spec.md) updated with reference-agent summary (PoC producer for data catalogs) and regenerable-projection vs original-record contrast.
* **Creation**: [architecture/knowledge-typology.md](/architecture/knowledge-typology.md) — types↔souls/instances derivation (invariance vs indexicality, consolidation stages, core vs role-grown); linked from [memory design](/architecture/memory-design.md).
* **Update**: framework package (agent-memory SKILL type-system section, okf SKILL freeform-type note, core.mjs scaffold index growth line, README) updated to carry the typology — approved by human this session.
* **Fix**: [roadmap/next-steps.md](/roadmap/next-steps.md) type corrected Task → Roadmap (more honest for a living plan).
* **Creation**: knowledge base seeded from the OAS founding session — [architecture](/architecture/), [decisions](/decisions/), the first-deployment record, [references](/references/), [roadmap](/roadmap/next-steps.md), [lessons](/lessons/), [playbooks](/playbooks/).
* **Initialization**: knowledge bundle scaffolded by oas.
